@model Parser.Data.ViewModels.Leaderboard.LeaderboardViewModel
@{
    Layout = null;
}

@Html.Partial("_LeaderboardViewModelPartial", Model)

@using (Ajax.BeginForm("Damage", new { pageNumber = Model.PageNumber }, new AjaxOptions()
{
    UpdateTargetId = "damage-done-models",
    InsertionMode = InsertionMode.Replace,
    HttpMethod = "POST"
}, new { id = "update-form" }))
{
    @Html.AntiForgeryToken()
}
